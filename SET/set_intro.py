# sets - anather data type collaction of items/elements
# sets are non-sequential collaction
# no indexing in sets
# comma saparated element closed within {}

# set does not allow duplicate elements

# usecase = anything is unique that case we can use set (passport,adhar no)

set1 = {1, 1,'python', 'git', 4}  #prints 1 only one time 
print(set1,type(set1))
print('set length',len(set1))
